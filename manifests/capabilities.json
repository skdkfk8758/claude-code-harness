{
  "schema_version": 2,
  "description": "CCH capability manifest — single source of truth for capabilities, health rules, and error codes",
  "sources": {
    "excalidraw": {
      "type": "plugin",
      "description": "Excalidraw diagram generation skill",
      "required": false,
      "health_rule": {
        "condition": "unavailable",
        "health": "Degraded",
        "reason_code": "RC_SRC_UNAVAILABLE",
        "impact": "diagram_capability_degraded"
      }
    }
  },
  "error_codes": {
    "RC_ALL_OK": { "severity": "healthy", "description": "All checks passed" },
    "RC_MISSING_DIR": { "severity": "blocked", "description": "Required directory missing" },
    "RC_SRC_UNAVAILABLE": { "severity": "degraded", "description": "Source unavailable" }
  }
}
